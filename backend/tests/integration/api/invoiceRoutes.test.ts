import { describe, it } from "vitest";

describe("Invoice Routes", () => {
  it.todo("should generate an invoice for a valid order");
  it.todo("should return 404 for non-existent order");
  it.todo("should return 404 when downloading invoice for order without one");
});
