import { describe, it } from "vitest";

describe("StorageService", () => {
  it.todo("should create and retrieve a chat order");
  it.todo("should enforce organization data isolation");
  it.todo("should soft-delete an order");
});
