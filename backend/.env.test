# ────────────────────────────────────────────────────────────────
# Chat2Cash — Test Environment Variables
# ────────────────────────────────────────────────────────────────
# DATABASE_URL and REDIS_URL are injected at runtime by
# Testcontainers (see tests/setup.ts). The placeholders below
# exist only so dotenv has something to parse on first load.
# ────────────────────────────────────────────────────────────────

NODE_ENV=test
PORT=0

# Database & Redis (overridden by Testcontainers at runtime)
DATABASE_URL=postgresql://test:test@localhost:5432/chat2cash_test
REDIS_URL=redis://localhost:6379

# AI Service — use a dummy key; mock Anthropic calls in tests
ANTHROPIC_API_KEY=sk-ant-test-placeholder-key
AI_MODEL_FAST=claude-3-haiku-20240307
AI_MODEL_SMART=claude-3-5-sonnet-20241022
AI_REQUEST_TIMEOUT_MS=5000

# Business Defaults
DEFAULT_GST_NUMBER=22AAAAA0000A1Z5
DEFAULT_BUSINESS_NAME=Chat2Cash Test Store

# Rate Limiting (relaxed for tests)
RATE_LIMIT_FREE=1000
RATE_LIMIT_PRO=1000
RATE_LIMIT_ENTERPRISE=1000
RATE_LIMIT_WINDOW_MS=900000

# Neon Auth — placeholder URLs; mock JWT verification in tests
NEON_AUTH_URL=https://test.neonauth.example.com
NEON_JWKS_URL=https://test.neonauth.example.com/auth/jwks

# Azure Blob Storage — fake credentials; mock storage in tests
AZURE_STORAGE_ACCOUNT_NAME=testaccount
AZURE_STORAGE_ACCOUNT_KEY=dGVzdGtleQ==
AZURE_STORAGE_CONTAINER_NAME=test-invoices
