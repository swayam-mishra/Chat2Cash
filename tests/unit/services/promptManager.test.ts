import { describe, it } from "vitest";

describe("promptManager", () => {
  it.todo("should build a valid extraction prompt from a single message");
  it.todo("should build a valid extraction prompt from chat messages");
});
