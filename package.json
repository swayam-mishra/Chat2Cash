{
  "name": "chat2cash-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "shared"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=backend",
    "db:push": "npm run db:push --workspace=backend"
  }
}